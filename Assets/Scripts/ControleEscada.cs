using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControleEscada : MonoBehaviour
{
    private Animator escadaAnmimator;

    // Start is called before the first frame update
    void Start()
    {
        escadaAnmimator = GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

 

}
